import React from "react"
import StyledButton from "./StyledButton"

const metadata = {
  component: StyledButton,
  title: "Buttons / StyledButton"
}

export default metadata

export const Example: React.VFC<{}> = () => <StyledButton />
