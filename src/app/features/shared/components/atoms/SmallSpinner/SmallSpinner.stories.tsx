import React from "react"
import SmallSpinner from "./SmallSpinner"

const metadata = {
  component: SmallSpinner,
  title: "Feedback / SmallSpinner"
}

export default metadata

export const Example: React.VFC<{}> = () => <SmallSpinner />
